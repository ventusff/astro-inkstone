---
title: The checks
brand: The checks
subtitle: "one gate per layer\nsource · links · output · render · contrast"
description: check-content, check-wikilinks, check-dist, ui_probe, contrast_probe — one gate per layer, each catching a class of silent failure that ships under a green build.
kind: reference
domains: [tooling]
tags: [ci, checks]
status: growing
created: 2026-08-01
updated: 2026-08-22
---

import Hero from 'astro-inkstone/components/Hero.astro';

<Hero kicker="Reference · tooling" title="The checks" meta="five checks · three from the engine, two from this package · all green before a commit">
  A green build does not mean the pages are fine. This toolchain places one check at each layer, each catching a class of "build green, page broken" silent failure — and two of them look at pages the way a reader does, in a real browser.
</Hero>

## Five gates, five layers

| Check | Ships with | Looks at | When to run |
| --- | --- | --- | --- |
| check-content | engine, `scripts/check-content.mjs` | every md/mdx source file | content-repo CI, or after writing |
| check-wikilinks | engine, `scripts/check-wikilinks.mjs` | the `[[wikilink]]` graph | content-repo CI, or after renaming a note |
| check-dist | engine, `scripts/check-dist.mjs` | the `astro build` output | after every build (postbuild) |
| ui_probe | this package, `scripts/ui_probe.mjs` | pages rendered in a real browser | after style/layout changes |
| contrast_probe | this package, `scripts/contrast_probe.mjs` | every text node's contrast, both themes | after any token change |

## check-content: the source layer

Compiles every md/mdx headed for a page with **the exact same dialect** as the site — syntax errors and silent deformations (emphasis markers that cannot pair, braces swallowed by MDX evaluation, list markers born from line wrapping, single-line `$$`, formulas KaTeX cannot render — the same set the [[kitchen-sink]] closes with) all turn CI red.

Why this script must come from the engine: a checker whose plugin set differs from the site's is **worse than no checker** — one missing the math plugin misreads formula braces as JSX expressions, one without GFM waves tables through. The dialect is written once in the engine; site rendering, CMS save validation and this script consume the same one, always.

```bash title="content repo root"
node <engine>/scripts/check-content.mjs . --glob '**/index.{md,mdx}' --math
```

Beyond compiling, it catches two frontmatter classes of silent loss: an unquoted `#` in a value (YAML reads it as a comment and silently truncates the rest) and CRLF line endings (the frontmatter regex only accepts LF, so titles silently fall back to slugs).

## check-wikilinks: the link graph

Dead `[[wikilinks]]` deliberately do not fail the build — a garden must be allowed to link to notes it hasn't grown yet. But link rot still belongs in CI, so the engine ships a lint that resolves every wikilink with **the library's own regex and resolution rules** (alias, brand, title, locale mirrors — zero drift by construction) and reports the missing, the ambiguous, and the dubious anchors:

```bash title="this garden's link check"
node vendor/astro-inkbrush/scripts/check-wikilinks.mjs src/content/notes --locale-prefix zh/
```

## check-dist: the output layer

In the built `dist/`, every internal reference a reader can click must actually exist. It catches the silent gaps beneath a green build:

- internal links/assets pointing at files that don't exist (the kind that appears by the dozen after a route reshuffle);
- in-page anchors pointing at ids that don't exist;
- doubled locale segments in paths (`/en/en/` — the classic result of i18n fallbacks stacking a prefix onto already-prefixed routes);
- `<a>` nested inside `<a>` (the HTML parser closes the outer one early and buttons fall out of their cards);
- KaTeX error residue (the formula is red text on the page while the build stays green).

This demo wires it into postbuild: a green `npm run build` means the output check passed too.

```bash title="site root, after a build"
node vendor/astro-inkbrush/scripts/check-dist.mjs dist
```

## The render-layer probe

Source and output can both be right while the page is still broken — the classic case being landing cards that no stylesheet rule matches, rendering as one crammed line of bare text: link and anchor checks stay green because they never look at a rendered page. `ui_probe` drives a real browser over every page in `dist` at four viewport widths (1440/1024/768/430) and measures: horizontal overflow, unstyled classes, dead anchors, skipped heading levels, images without alt. It reports only what a machine can prove — no aesthetic judgment.

```bash title="needs a local Chrome/Chromium"
npm run build
node ../scripts/ui_probe.mjs dist   # serves dist itself; pass a baseUrl to probe a live server
```

Green means the report's last line reads `PAGES WITH FINDINGS: 0`.

## The contrast probe

The tokens claim AA, so the claim is measured rather than asserted. `contrast_probe` loads every page of `dist` in a real browser, in the light theme and again in the dark one, and for every visible text node computes the WCAG ratio between its rendered color and the ground it actually sits on — the nearest opaque ancestor background, with translucent layers composited in. Small text is held to 4.5:1, large text (18px bold, or 24px) to 3:1; the report names the page, the selector, the two colors and the ratio of every node under the bar. Tinted chips, `color-mix()` grounds and the night palette are all measured as rendered, so a token change that quietly drops a label to 3.8:1 shows up the same evening:

```bash title="after a build, both themes"
node ../scripts/contrast_probe.mjs dist
```

<div class="callout system">
  <div class="callout-title">The style-change routine</div>
  Any change to the package's stylesheets or components goes through this demo's build first, then <code>ui_probe</code> and <code>contrast_probe</code> over the output — all green before committing. The demo doubles as the package's test bed.
</div>
