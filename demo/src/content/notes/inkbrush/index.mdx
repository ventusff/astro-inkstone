---
title: The editing machine
description: "A five-chapter tour of astro-inkbrush, the git-backed CMS engine this garden pairs with — and this very site is its demo: in editing mode, every page here is editable in place."
brand: inkbrush
subtitle: "the CMS engine, on its own manual\nfive chapters · one hub"
kind: reference
domains: [editing]
tags: [inkbrush]
status: evergreen
created: 2026-08-24
updated: 2026-08-24
navLabel: Hub · Overview
chapters: false
nav:
  - group: The engine
    pages: [editing, revisions, ai-assist, inbox, wikilinks]
---

import Hero from 'astro-inkstone/components/Hero.astro';
import HubCard from 'astro-inkstone/components/HubCard.astro';
import Stats from 'astro-inkstone/components/Stats.astro';

export const base = import.meta.env.BASE_URL.replace(/\/$/, '');

<Hero kicker="Reference · the CMS engine" title="The editing machine" meta="five chapters · zero production bytes · this site is the demo">
  [astro-inkstone](https://github.com/ventusff/astro-inkstone) is the paper; [astro-inkbrush](https://github.com/ventusff/astro-inkbrush) is the pen. The engine turns any Astro content site into its own editor: hover a paragraph in editing mode and a small handle offers ✎ edit, ✦ ask Claude, ⟲ history. There is no separate demo site to go look at — **the garden you are reading is the demo**. Run it with `npm run wiki` and every note here, this page included, opens for editing in place; a static build like the one you are reading contains not a single byte of CMS code.
</Hero>

<Stats
  items={[
    { value: 'git', label: 'the only database' },
    { value: '2', label: 'safety nets on every save' },
    { value: '0 B', label: 'CMS code in a static build' },
  ]}
/>

## Five chapters `{#chapters notoc}`

<div class="hub-grid">
  <HubCard href={`${base}/inkbrush/editing/`} num="PART I" title="Editing, block by block" count="✎"
    desc="CodeMirror in place of the block you are reading, a live preview through the site's own pipeline, and two safety nets before anything touches disk." />
  <HubCard href={`${base}/inkbrush/revisions/`} num="PART II" title="Revision history and revert" count="⟲"
    desc="Every save journaled per block — who, when, the exact before and after — with one-click, itself-revertible revert." />
  <HubCard href={`${base}/inkbrush/ai-assist/`} num="PART III" title="AI assist" count="✦"
    desc="Block rewrites, a note-aware chat and whole-note translation through the claude CLI, sandboxed to a throwaway copy." />
  <HubCard href={`${base}/inkbrush/inbox/`} num="PART IV" title="The Obsidian inbox" count="📥"
    desc="Point the watcher at a vault folder; new notes convert and import themselves — embeds, wikilinks, highlights included." />
  <HubCard href={`${base}/inkbrush/wikilinks/`} num="PART V" title="How wikilinks resolve" count="[[ ]]"
    desc="One resolver for the pages, the editor preview, the importer and the CI lint — locale mirrors first, then ids, aliases, brands and titles." />
</div>

## Where it sits

The engine owns the CMS features **and** the Markdown dialect with its content guard — the grammar the editor accepts and the grammar the page renders must be one grammar, so it is written once and consumed everywhere ([[three-way-split|the three-way split]] explains why the line is drawn there). Styling belongs to the paper layer; identity belongs to your site. Everything rendered on these pages — the strip above this text included — is the paper at work; everything that *edits* these pages is the engine.

<div class="path">
  <strong>Reading path.</strong>
  New to the engine → <a href={`${base}/inkbrush/editing/`}>Part I</a><span class="arrow">→</span><a href={`${base}/inkbrush/revisions/`}>Part II</a>; wiring it into a site → the <a href={`${base}/getting-started/`}>getting-started guide</a> first; the dialect the save gate enforces → the <a href={`${base}/kitchen-sink/`}>kitchen sink</a>; a note about keeping notes → <a href={`${base}/garden-craft/`}>the gardening essay</a>.
</div>
