---
/**
 * Steps / pipeline / static timeline container. Put a markdown list inside;
 * items render on a vertical rail with dots (base.css `.steps-wrap`).
 */
---

<div class="steps-wrap">
  <slot />
</div>
