node_modules/
dist/
.astro/

# demo site runtime state (CMS data and per-machine config — never committed)
demo/.wiki/
demo/inkbrush.config.ts

# intermediate artifacts of the font subset build
fonts/*.ttf

# probe reports are run artifacts, never tracked
ui-probe.txt
contrast-probe.txt

# Python bytecode from the font subset script
__pycache__/
