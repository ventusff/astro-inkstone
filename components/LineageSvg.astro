---
/** Framed figure for hand-authored SVG diagrams (lineage trees, taxonomies). */
interface Props {
  title?: string | undefined;
}
const { title } = Astro.props;
---

<figure class="lineage">
  {title && <figcaption class="lineage-title">{title}</figcaption>}
  <slot />
</figure>
